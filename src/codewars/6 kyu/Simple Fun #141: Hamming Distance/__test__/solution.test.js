const solution = require('../solution')

describe('', () => {
    test('', () => {
        expect(solution(25, 87)).toEqual(4)
        expect(solution(256, 302)).toEqual(4)
        expect(solution(543, 634)).toEqual(4)
        expect(solution(34013, 702)).toEqual(7)
    })
})
