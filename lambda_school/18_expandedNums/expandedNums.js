/* ———
 * Write a function that accepts a number and returns it in a string as its expanded form.
 * Example: Given the number 562 your function should return '500 + 60 + 2'.
 * ——— */

const expandedNums = n => {
  // Code here
}

console.log(expandedNums(562)) // 500 + 60 + 2
console.log(expandedNums(5620)) // 5000 + 600 + 20
console.log(expandedNums(50602)) // 50000 + 600 + 2