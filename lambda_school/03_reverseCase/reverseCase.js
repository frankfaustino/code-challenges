/* ———
 * Write a function that takes a string as input and reverses the letter case of that string.
 * ——— */

const reverseCase = str => {
  // Code here
}

/* ——— Test case ——— */
console.log(reverseCase('HelloWorld')) // -> 'hELLOwORLD'